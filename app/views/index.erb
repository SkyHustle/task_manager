<body>
<!-- Header -->
<a name="about"></a>
<div class="intro-header">
    <div class="container">

        <div class="row">
            <div class="col-lg-12">
                <div class="intro-message">
                    <h1>All Tasks</h1>
                    <hr class="intro-divider">
                      <% @tasks.each do |task| %>
                        <ul class="list-inline intro-social-buttons">
                            <li>
                                <a href="/tasks/<%= task.id %>" class="btn btn-default btn-lg"><%= task.id %>:</i> <span class="network-name"><%= task.title %></span></a>
                            </li>
                        </ul>
                      <% end %>
                </div>
            </div>
        </div>
    </div>
</div>
</body>